# TODO

## Media Support

- [ ] `scribe show` — render inline images as Sixel between text blocks (via `img2sixel`)
- [ ] `scribe attachments "note name"` — extract attachments to temp dir, optionally display as Sixel
- [ ] `scribe edit` — warn/guard when a note has attachments to avoid nuking them (add `--force` flag to override)
